FastDP Setup
Run for fastDP: pip install git+https://github.com/awslabs/fast-differential-privacy.git
Run for torchP: pip3 install torch torchvision --index-url https://download.pytorch.org/whl/cu126

FlashDP Setup
Run for FlashDP: git clone https://github.com/kaustpradalab/flashdp.git
cd flashdp
conda env create -f env.yml # Optional: Execute only if you wish to create a new Conda environment.
conda activate flashdp      # Optional: Run if the above command is executed.
bash install.sh